# PRD：同频 — 学习状态伙伴

> 这是 `同频` 项目的第一份产品需求文档（PRD）。文档结构对照第 5 天的原型、第 6 天的代码、第 7 天的发布（Day 7 MVP）来设计。源头是 Day 3 的 `research.md`。

---

## 0. 项目元信息

| 项 | 内容 |
|---|---|
| 项目名 | **同频** |
| 一句话定位 | 到点提醒 + 手动打卡 + 日复盘，让"一个人的自习"有个"懂你的伙伴" |
| 目标用户 | 自律差、独自学习的大学生 / 自由职业者 |
| 核心价值 | 把"记录派"的温和复盘（无人在场）和"强制派"的提醒督促（无长期复盘）合在一起 |
| Day 7 MVP 边界 | **到点提醒 + 手动打卡 + 日视图** 三件事。其他全部不做。 |

---

## 1. 用户故事

### 1.1 故事 A — 没工具的我（Before）

> 小林（化名）今天计划学 4 小时。早上 9 点坐到桌前，打开手机看了 30 分钟，然后告诉自己"晚上开始也行"。下午被朋友群消息打断，刷到傍晚 6 点。一天结束他躺在床上刷到睡着——既没开始，也不知道今天到底浪费在哪，陷入"我是不是就这样废了"的自我怀疑。

**痛点**：一个人、没人在场、事后无据可查。

### 1.2 故事 B — 用了 `同频` 的我（After）

> 早上 9 点小林坐下 1 分钟后，`同频`浏览器通知弹出："9:30 第一次提醒 — 今天打算做什么？点我开始"。他点"开始"。
> 12:00，午饭前通知："已坚持 2.5 小时，第一段最难的部分你已经撑过去了。"
> 下午 4 点他刷手机刷得不亦乐乎，`同频`来一条："这已经第 3 小时了。离一个小习惯，还差一天。"
> 晚上 9 点收尾打卡："给今天打个分（1-5），写一句今天的话"。他写"前半天拖死，下午挤了 2 小时出来，3 分"。
> 关电脑前，他点开"日复盘"，看到今天 4 张打卡卡片 + 时间轴 + 平均 3.0 分 + 一句系统自动摘出来的"今日总结"。

**对照说明**：故事 A 里"一天结束不知道自己做了什么"，故事 B 里"看一眼就知道、而且有鼓励在场"——这就是产品差异点。

---

## 2. 页面与视图清单（共 3 个，符合"至少 3 个页面或视图"的完成后检查）

### P1 — 今日页（核心入口）

- 顶部：今天日期 / 当前时间 / "下一提醒还有 N 分钟"
- 中部：今日已打卡的卡片列表（按时间排，每张含分数 + 备注 + 时间）
- 底部：主操作区
  - 「打卡」按钮（手动触发打卡弹窗）
  - 「打开日复盘」按钮（展开 P3）
- 顶部右下角小角标：今日完成度（已完成 / 应完成）

### P2 — 打卡弹窗（强制交互，模态）

- 触发：今日页「打卡」按钮 或 浏览器通知点入
- 内容：
  - 标题："给现在打个分"
  - 分数滑动条（1-5，必填）
  - 一句话备注框（选填，最多 100 字）
  - 「保存」按钮（分数未选时禁用）
  - 「跳过（今天没做）」按钮（淡色，仅用于补卡场景）
- 保存后：弹窗关闭，自动回到触发源（P1 或通知状态）

### P3 — 日复盘视图（折叠面板，嵌在 P1 内）

- 入口：P1 底部「打开日复盘」
- 内容：
  - 时间轴（按小时排的今日打卡，每张含分数 + 时间 + 备注）
  - 完成度进度条（"今日打了 N / 应打 M"）
  - 平均分
  - 「今日总结」一行（从今日所有备注里抽一句最长的）

---

## 3. MVP 功能清单（每个功能 1 天内可拆）

| 编号 | 功能 | 单日可拆的理由 |
|---|---|---|
| **F1** | 浏览器通知（到点提醒） | 只用 `Notification` API + 一个 setTimeout / setInterval，文案从文案库抽 |
| **F2** | 手动打卡 | 一个表单 + 一个 localStorage 写入，2-3 小时就能跑通 |
| **F3** | 日视图 | 纯前端从 localStorage 读出排序渲染，无外部依赖 |
| **F4** | 本地存储（localStorage） | 一行 `JSON.stringify` / `JSON.parse`，10 分钟 |

**为什么每个都能 1 天内拆**：每个都是单一职责、单一文件、不依赖后端，Day 6 一个上午能写完。

---

## 4. 验收标准（Acceptance Criteria）

> 格式："谁 / 在什么页面 / 做什么 / 看到什么"

### F1 — 浏览器通知
- **谁**：用户
- **在什么页面**：P1（今日页，页面已停留 ≥ 30 秒）
- **做什么**：等到下一个整点（默认每小时一次，hardcode 在 `src/copy.js` 的 schedule 字段；用户自定义频率留到 Day 8+）
- **看到什么**：浏览器右上角弹出系统通知，标题 `同频 · 时间到了`，正文从文案库（5-8 套混合调性）随机抽一条。点通知 → 唤起 P2 打卡弹窗。

### F2 — 手动打卡
- **谁**：用户
- **在什么页面**：P1 今日页（点「打卡」按钮）或 通知点入（唤起 P2）
- **做什么**：滑动 1-5 分数条 → 选填备注 → 点「保存」
- **看到什么**：弹窗自动关闭，P1 多一张最新打卡卡片，"今日完成度"小角标 +1，"下一提醒还有 N 分钟"重新计时。

### F3 — 日视图
- **谁**：用户
- **在什么页面**：P1 今日页
- **做什么**：点「打开日复盘」
- **看到什么**：P3 折叠面板展开，列出今日所有打卡卡片（按时间排序），下方三行：完成度 N/M、平均分 X.X、今日总结（系统从今日最长备注里摘一句展示）。

### F4 — 本地存储
- **谁**：用户
- **在什么页面**：P1 / P3
- **做什么**：刷新浏览器
- **看到什么**：之前打的卡全部还在（数据持久化生效）。打开 DevTools → Application → Local Storage 能看到 `tb_records_v1` 这个 key。

### 完成后检查（实战步骤里给的 3 条，全部满足）

| # | 检查项 | 满足证据 |
|---|---|---|
| 1 | 随机挑一个功能，能回答"谁 / 在什么页面 / 做什么 / 看到什么" | F1-F4 全部已写 |
| 2 | PRD 有 MVP 和后续分界 | 第 0 节"Day 7 MVP 边界" + 第 5 节「本期不做」清单 |
| 3 | 至少 3 个页面或视图 | 第 2 节设计了 3 个：P1 今日页 / P2 打卡弹窗 / P3 日复盘视图 |

---

## 5. 本期不做（继承 `research.md` 第 6 节）

- ❌ 社交功能（好友 PK、排行榜）
- ❌ 系统级自动监控（读手机使用时长、步数）
- ❌ AI 深度伙伴（个性化上下文记忆、情绪感知）  — **见下方 5.1**
- ❌ 多端同步
- ❌ 付费 / 会员体系
- ❌ 数据导出与导入
- ❌ 周 / 月 / 年复盘视图（Day 7 MVP 仅做日视图）

### 5.1 关于「AI 深度伙伴」的补充说明

研究文档里它是产品的灵魂，**MVP 不上**，但保留一条逃生通道：

- Day 6 准备文案库时，文案**不是**纯随机，而是按"今日完成度"+"上次打卡分数"两个简单信号触发（这已经算"懂用户"的最轻量形式）。
- 真正的"AI 伙伴"留到 Day 8 之后讨论，本周不主动加。

---

## 6. 风险与约束

### 6.1 浏览器通知的可达性
- `Notification.requestPermission()` 必须用户点击「允许」，第一次要写引导页
- 大部分浏览器要求 HTTPS 或 localhost 才能弹通知；**Day 6 部署到 EdgeOne Pages 后必须做一次真实可达性测试**
- 若 EdgeOne Pages 部署后通知不可达，**降级方案**：改为"页面内 toast + 系统声音"组合，依然守住"提醒"这个核心差异

### 6.2 文案库规模（混合调性 = 理性 + 暖心 + 毒舌）
- 理性派 3 套（如 "已坚持 N 天，离一个小习惯还差 M 天"）
- 暖心派 3 套（如 "今天你也撑过来了，我知道不容易"）
- 毒舌派 2 套（如 "刷手机很爽吧，但你今天还有 N 个打卡没做"）
- 合计 8 套够 MVP 一周内不重复。Day 6 准备清单写在 `src/copy.js` 里。

### 6.3 数据备份是已知风险
- MVP 用了 localStorage，**清浏览器缓存会丢数据**
- 在 `index.html` 帮助说明里写一句"建议每周截图备份（未来支持导出）"
- 本期接受，不在本 PRD 范围

### 6.4 通知点入唤起 P2 的降级路径
- **首选**：用 `Service Worker` 的 `notificationclick` 监听事件，直接调起 P2 弹窗
- **降级**：若 Service Worker 不可达（如部署到 EdgeOne Pages 后某些路径下），通知携带 `?checkin=1` URL 参数，点入后 P1 在挂载时检测到该参数自动唤起 P2 弹窗（无需额外代码，URL 参数走 history.replaceState 也行）
- **最差兜底**：去掉"通知点入唤起 P2"这条验收，改为只走"今日页点「打卡」按钮"；验收标准相应删掉"或通知点入"那一项

---

## 7. 里程碑（Day 5-7 怎么拆）

### Day 5 — 原型（无代码）
- 画出 3 个页面的线框（手绘 / Excalidraw 都行）
- 把"用户故事 A/B → 页面元素"的映射表写出来（每条故事用到了 P1/P2/P3 哪些元素）
- 验收：别人看线框能复述产品做什么

### Day 6 — 代码（实施）
- 实现 F1-F4 + 文案库 + localStorage
- 每完成一个功能就回 PRD 第 4 节**自测对应一条**验收标准
- 验收：本地能跑通 F1-F4 全链路

### Day 7 — 发布（MVP）
- 部署到 EdgeOne Pages（公网 HTTPS 域名）
- 真实浏览器通知可达性测试
- 把首页链接发到班级群
- 验收：群里其他人点开能看到今日页、能收到一次提醒、能打卡

---

## 8. 每日一问（今天的问题）

> **你在 PRD 里砍掉了什么功能？为什么砍它？**

`研究文档` 里我列了 3 个候选 MVP 边界（最小可用 / 最小可用+周月视图 / 最小可用+个性化记录），今天我**只选了最小可用**，砍掉了：

- **周 / 月 / 年复盘视图** — 因为写一份"月度统计"的前端组件至少比单纯"日视图"多花半天，对自律差的人最大价值是"今天坚持没坚持"，月度价值暂时看不见。
- **个性化上下文记录**（"我今天状态变差了"这种自我标注） — 因为它会让打卡表单从"打 1 个分数"变成"打 1 个分数 + 选标签 + 写状态"，首日就会卡在"今天我不确定自己状态怎么分类"，反而打断了提醒督促的主流程。

**砍它们的共同准则**：所有不影响"今天有人提醒我、看到我打卡"这条主线的功能，一律留到 Day 8 之后再说。

---

> 文档结束。下一步：AI 自检（板块 ③），看挑不挑得出「必须修改」级问题。
